# Frontend API base URL for Vite
# Use /api in development together with the Vite proxy below.
VITE_API_BASE_URL=/api

# Backend server used by the Vite dev proxy.
VITE_API_PROXY_TARGET=http://localhost:4000
